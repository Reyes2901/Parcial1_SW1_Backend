# {{projectName}}

Proyecto Spring Boot generado automáticamente por CASE UML Platform.

## Requisitos
- Java 17+
- Maven 3.9+
- PostgreSQL 14+

## Base de datos
{{databaseDescription}}

## Ejecución
```bash
mvn spring-boot:run
```
El servidor arranca en http://localhost:8080.

## Endpoints disponibles
{{#each entities}}

### {{pascalName}}
- GET /api/v1/{{pluralKebabName}} — Lista
- GET /api/v1/{{pluralKebabName}}/:id — Obtener por ID
- POST /api/v1/{{pluralKebabName}} — Crear
- PUT /api/v1/{{pluralKebabName}}/:id — Actualizar
- DELETE /api/v1/{{pluralKebabName}}/:id — Eliminar
{{/each}}

## Pruebas con Postman
1. Abre Postman Desktop.
2. Import → File → postman_collection.json.
3. Ejecuta las requests.
