const fs = require('fs'); const file = 'src/server.ts'; let code = fs.readFileSync(file, 'utf8'); if (!code.includes('collaborationWsRoutes')) { code = code.replace('import { generationsRoutes } from './modules/generations/generations.routes';', 'import { generationsRoutes } from './modules/generations/generations.routes';\nimport websocket from '@fastify/websocket';\nimport { collaborationWsRoutes } from './modules/collaboration/ws.routes';').replace('server.register(generationsRoutes);', 'server.register(websocket);\n  server.register(collaborationWsRoutes);\n  server.register(generationsRoutes);'); fs.writeFileSync(file, code, 'utf8'); console.log('SUCCESS'); }  
